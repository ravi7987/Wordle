export enum AppEnv {
	Production = "production",
	Dev = "dev",
}

export enum AppType {
	Default = "default",
}

export enum DataBase {
	POSTGRES = "postgres",
	MONGODB = "mongodb",
	MYSQL = "mysql",
	MSSQL = "mssql",
}
